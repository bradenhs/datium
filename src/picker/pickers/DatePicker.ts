class DatePicker implements IPicker {
    constructor(private element:Element) {
        
    }
    
    public updateOptions(options:IOptions) {
        
    }
    
    public getHeight() {
        return 200;
    }
}